import React from 'react';

import HomeFeed from '../components/HomeFeed';

const Home = () => {
  return (
    <div
      className="timeline"
      style={{
        padding: '0 8px'
      }}
    >
      <HomeFeed />
    </div>
  );
};

export default Home;
